class Node {
    baum = {

    }

    constructor(height, width) {
      this.height = height;
      this.width = width;
    }

    insert(node) {
        this.baum["child"] = "Hello1";
        console.log(this.baum);
    }
  }