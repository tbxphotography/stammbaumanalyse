class Node {
    baum = {

    }

    constructor(height, width) {
      this.height = height;
      this.width = width;
    }

    insert(node) {
        this.baum.push("Hello1");
        console.log(this.baum);
    }
  }